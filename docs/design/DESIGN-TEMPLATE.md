# 設計: <機能名>

> **ステータス**: 草稿 / レビュー中 / 承認済み  
> **作成日**: YYYY-MM-DD  
> **最終更新**: YYYY-MM-DD  
> **担当者**: @<GitHubユーザー名>  
> **関連 Issue**: #<Issue番号>

---

## 対応仕様書

- [`SPEC-<機能名>.md`](../specs/SPEC-<機能名>.md)

---

## コンポーネント設計

<!-- 新規追加・変更する React コンポーネントを記述してください。 -->
<!-- 既存コンポーネントへの影響がある場合も記載してください。 -->

### コンポーネント構成図

```
<ParentComponent>
├── <ChildComponent1>   # 〇〇の役割
└── <ChildComponent2>   # △△の役割
    └── <GrandChild>    # □□の役割
```

### 新規コンポーネント

#### `<ComponentName>`

- **ファイルパス**: `components/<ディレクトリ>/<ComponentName>.tsx`
- **責務**: このコンポーネントが担う役割
- **Props**:

```typescript
type <ComponentName>Props = {
  propName: PropType;  // 説明
};
```

- **状態管理**: useState / useContext / Zustand など使用するものを記述
- **注意事項**: パフォーマンス上の考慮点・副作用など

### 変更するコンポーネント

#### `<ExistingComponentName>`

- **ファイルパス**: `components/<ディレクトリ>/<ExistingComponentName>.tsx`
- **変更内容**: 何をどう変えるか
- **影響範囲**: 変更による他コンポーネントへの影響

---

## 型定義変更

<!-- TypeScript の型・インターフェース・Zod スキーマの追加・変更を記述してください。 -->
<!-- ファイルパスとともに記載してください。 -->

### 追加する型

```typescript
// lib/types.ts に追加
export type NewType = {
  id: string;
  name: string;
  // ...
};
```

### 変更する型

```typescript
// lib/types.ts の既存の型を変更
// Before:
export type ExistingType = {
  id: string;
};

// After:
export type ExistingType = {
  id: string;
  newField: string; // 追加
};
```

---

## ストレージ変更

<!-- localStorage / IndexedDB などのクライアントサイドストレージの変更を記述してください。 -->
<!-- 変更がない場合は「なし」と記載してください。 -->

### localStorage

| キー             | 型           | 説明                         | 変更種別 |
| ---------------- | ------------ | ---------------------------- | -------- |
| `apiTester_〇〇` | `TypeName[]` | 〇〇の一覧データ             | 追加     |

### データ構造

```typescript
// 保存するデータの型
type StoredData = {
  version: number; // スキーマバージョン（マイグレーション用）
  items: Item[];
};
```

### マイグレーション方針

- 例: バージョンが存在しない既存データは `version: 1` として扱い、`newField` に `""` を補完する

---

## API ルート変更

<!-- Next.js の API Route（app/api/ 配下）の追加・変更を記述してください。 -->
<!-- サーバー API を持たない場合（クライアントサイドのみの変更）は「なし」と記載してください。 -->

### 追加するエンドポイント

#### `GET /api/<パス>`

- **ファイルパス**: `app/api/<パス>/route.ts`
- **説明**: 〇〇の一覧を返す
- **リクエスト**: クエリパラメータなし
- **レスポンス**:

```typescript
type Response = {
  items: Item[];
};
```

#### `POST /api/<パス>`

- **ファイルパス**: `app/api/<パス>/route.ts`
- **説明**: 〇〇を作成する
- **リクエストボディ**:

```typescript
type RequestBody = {
  name: string;
};
```

- **レスポンス**:

```typescript
type Response = {
  id: string;
  name: string;
  createdAt: string;
};
```

---

## 実装手順

<!-- 作業を細分化したステップを記述してください。 -->
<!-- PR 分割の指針にもなるため、なるべく独立して動作する単位に分けることを推奨します。 -->

### Step 1: 型定義の追加・変更

- [ ] `lib/types.ts` に新しい型を追加する
- [ ] 既存の型の破壊的変更に対応する（型エラーを修正する）

### Step 2: ストレージ層の実装

- [ ] `lib/storage.ts` に CRUD 関数を追加する
- [ ] マイグレーション処理を実装する

### Step 3: コンポーネントの実装

- [ ] `<ComponentName>` コンポーネントを新規作成する
- [ ] `<ExistingComponentName>` コンポーネントを変更する

### Step 4: 統合・結合

- [ ] ルートコンポーネントに組み込む
- [ ] 動作確認を行う

### Step 5: テスト追加

- [ ] ユニットテストを追加する
- [ ] 手動動作確認のチェックリストを実施する

---

## テスト計画

<!-- テストの方針と主なテストケースを記述してください。 -->

### ユニットテスト

| 対象                     | テストケース                             | ファイルパス                          |
| ------------------------ | ---------------------------------------- | ------------------------------------- |
| `<functionName>()`       | 正常系: 〇〇を渡すと△△を返す           | `__tests__/<functionName>.test.ts`    |
| `<functionName>()`       | 異常系: 不正な値を渡すとエラーになる     | `__tests__/<functionName>.test.ts`    |

### 統合テスト（コンポーネントテスト）

| 対象                     | テストケース                             |
| ------------------------ | ---------------------------------------- |
| `<ComponentName>`        | 〇〇を表示する                           |
| `<ComponentName>`        | 〇〇をクリックすると△△が呼ばれる        |

### 手動確認チェックリスト

- [ ] 〇〇を操作して△△が表示されることを確認
- [ ] ページをリロードしてデータが保持されていることを確認
- [ ] エラー系（〇〇が空の場合など）の表示を確認

---

## 備考

<!-- 技術的な決定事項・代替案の検討結果・将来の拡張に向けた注意点などを記述してください。 -->

- **決定事項**:
  - 例: 状態管理に Zustand ではなく React Context を採用。理由: 規模が小さく過剰なため。
- **代替案**:
  - 例: 〇〇方式も検討したが、△△の理由で不採用。
- **将来の拡張**:
  - 例: 将来的に□□機能を追加する際は◇◇を考慮すること。
- **関連 PR**: #
